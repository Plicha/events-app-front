# Lib

Utility functions and shared code.

## Structure

- `api/` - API client and API-related functions
- `utils/` - Helper functions (date formatting, validation, etc.)
- `constants/` - Application constants and configuration

